/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'greatcontent.com' // added as requested
    ],
  },
};

export default nextConfig;


